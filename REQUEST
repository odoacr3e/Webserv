POST /upload/success_upload.html HTTP/1.1
Host: localhost:9020
Connection: keep-alive
Content-Length: 3692
Cache-Control: max-age=0
sec-ch-ua: "Not(A:Brand";v="8", "Chromium";v="144", "Brave";v="144"
sec-ch-ua-mobile: ?0
sec-ch-ua-platform: "Linux"
Upgrade-Insecure-Requests: 1
User-Agent: Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/144.0.0.0 Safari/537.36
Origin: http://localhost:9020
Content-Type: multipart/form-data; boundary=----WebKitFormBoundarytP3bgMO8AjuGS5AN
Accept: text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8
Sec-GPC: 1
Accept-Language: en-US,en;q=0.5
Sec-Fetch-Site: same-origin
Sec-Fetch-Mode: navigate
Sec-Fetch-User: ?1
Sec-Fetch-Dest: document
Referer: http://localhost:9020/upload/success_upload.html
Accept-Encoding: gzip, deflate, br, zstd

------WebKitFormBoundarytP3bgMO8AjuGS5AN
Content-Disposition: form-data; name="file"; filename="script.sh"
Content-Type: application/x-shellscript

# #!/bin/bash

set -e

# Directory necessarie
mkdir -p /var/lib/mysql /var/run/mysqld
chown -R mysql:mysql /var/lib/mysql /var/run/mysqld

# Se MariaDB non è inizializzato
if [ ! -d /var/lib/mysql/mysql ]; then
    echo ">> Inizializzazione MariaDB (prima installazione)..."
    mysqld --initialize-insecure --user=mysql --datadir=/var/lib/mysql
fi

# Se ci sono variabili da configurare, genero init.sql
INIT_FILE=""

if [ -n "$MYSQL_ROOT_PASSWORD" ] || [ -n "$MYSQL_DATABASE" ] || [ -n "$MYSQL_USER" ]; then
    INIT_FILE="/tmp/init.sql"
    echo "SET @@SESSION.SQL_LOG_BIN=0;" > "$INIT_FILE"

    # Password root
    if [ -n "$MYSQL_ROOT_PASSWORD" ]; then
        echo "ALTER USER 'root'@'localhost' IDENTIFIED BY '${MYSQL_ROOT_PASSWORD}';" >> "$INIT_FILE"
    fi

    # Database
    if [ -n "$MYSQL_DATABASE" ]; then
        echo "CREATE DATABASE IF NOT EXISTS \`${MYSQL_DATABASE}\`;" >> "$INIT_FILE"
    fi

    # Utente applicazione
    if [ -n "$MYSQL_USER" ] && [ -n "$MYSQL_PASSWORD" ]; then
        echo "CREATE 